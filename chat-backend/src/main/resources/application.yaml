spring:
  application:
    name: chat-backend

  datasource:
    url: jdbc:postgresql://localhost:5432/chatdb
    username: fernandovelazco
    password: Colombia.123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true

  sql:
    init:
      mode: always

  jackson:
    date-format: "yyyy-MM-dd HH:mm:ss"
    time-zone: "UTC"

# DESACTIVAR SEGURIDAD TEMPORALMENTE
security:
  basic:
    enabled: true
  enabled: true

app:
  security:
    enabled: false # Nuestra bandera personalizada
    development-mode: true # Modo desarrollo

  jwt:
    secret: "dGhpcy1pcy1hLXZlcnktc2VjdXJlLXNlY3JldC1rZXktZm9yLWp3dC10b2tlbi1nZW5lcmF0aW9u"
    expiration: 86400000

server:
  port: 8081

logging:
  level:
    com.chatapp: DEBUG
    org.springframework: INFO
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
