spring:
  application:
    name: chat-backend

  datasource:
    url: jdbc:postgresql://localhost:5432/chatdb
    username: fernandovelazco
    password: Colombia.123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true

  sql:
    init:
      mode: always

  jackson:
    date-format: "yyyy-MM-dd HH:mm:ss"
    time-zone: "UTC"

server:
  port: 8081
  servlet:
    context-path: /api

app:
  jwt:
    secret: "dGhpcy1pcy1hLXZlcnktc2VjdXJlLXNlY3JldC1rZXktZm9yLWp3dC10b2tlbi1nZW5lcmF0aW9u"
    expiration: 86400000
  cors:
    allowed-origins: "http://localhost:3000"

logging:
  level:
    com.chatapp: DEBUG
    org.springframework.web: DEBUG # Cambiado a DEBUG
    org.springframework.security: DEBUG # Cambiado a DEBUG
    org.springframework.security.web: DEBUG # Nuevo
    org.springframework.messaging: DEBUG # Nuevo para WebSocket
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n" # Mejor formato
